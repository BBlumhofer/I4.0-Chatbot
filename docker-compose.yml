services:
  api:
    build: .
    ports:
      - "8000:8000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      - NEO4J_URI=neo4j+s://database.neo4j.phuket.plant.smartfactory.de:7687
      - NEO4J_USER=neo4j
      - NEO4J_PASSWORD=testtest
      - REDIS_URL=redis://redis:6379
      - KAFKA_BOOTSTRAP_SERVERS=172.17.200.155:9092
      - OPCUA_ENDPOINT=opc.tcp://172.17.54.3:4842
      - OPCUA_CREDENTIALS_FILE=/app/cred.json
      - OPCUA_ENABLE_WRITE_EXECUTE=false
      - LLM_BASE_URL=http://host.docker.internal:11434/v1
      - LLM_MODEL=qwen3:32b
      - LLM_TIMEOUT_SECONDS=60
      - CHAT_ENABLE_LLM_TOOL_RESULT_SUMMARIZATION=false
      - EMBEDDING_PROVIDER=ollama
      - EMBEDDING_MODEL=embeddinggemma:latest
      - CHROMA_HOST=chroma
      - CHROMA_PORT=8000
      - AGENT_REGISTRY_URL=https://agent-registry.phuket.plant.smartfactory.de/
      - AGENT_REGISTRY_KEY=
      - AGENT_REGISTRY_TIMEOUT_SECONDS=15
    depends_on:
      - redis
      - chroma
    restart: unless-stopped

  open-webui:
    image: ghcr.io/open-webui/open-webui:main
    ports:
      - "3000:8080"
    environment:
      # Disable built-in Ollama integration – we provide our own OpenAI-compatible API.
      - OLLAMA_BASE_URL=""
      # Point Open WebUI at our FastAPI backend.
      - OPENAI_API_BASE_URL=http://api:8000/v1
      - OPENAI_API_KEY=i4-chatbot
      # Disable user authentication for local/internal deployments.
      - WEBUI_AUTH=false
      # Persist user data and settings across container restarts.
      - DATA_DIR=/app/backend/data
    volumes:
      - open_webui_data:/app/backend/data
    depends_on:
      - api
    restart: unless-stopped

 # neo4j:
 #   image: neo4j:5
 #   ports:
 #     - "7474:7474"
 #     - "7687:7687"
 #   environment:
 #     - NEO4J_AUTH=neo4j/password
 #   volumes:
 #     - neo4j_data:/data
 #   restart: unless-stopped

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    restart: unless-stopped

# zookeeper:
#   image: confluentinc/cp-zookeeper:7.6.0
#   environment:
#     ZOOKEEPER_CLIENT_PORT: 2181
#   restart: unless-stopped

# kafka:
#   image: confluentinc/cp-kafka:7.6.0
#   ports:
#     - "9092:9092"
#   environment:
#     KAFKA_BROKER_ID: 1
#     KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
#     KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092
#     KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
#   depends_on:
#     - zookeeper
#   restart: unless-stopped

  chroma:
    image: chromadb/chroma:latest
    ports:
      - "8001:8000"
    volumes:
      - chroma_data:/chroma/chroma
    restart: unless-stopped

volumes:
  neo4j_data:
  chroma_data:
  open_webui_data:

